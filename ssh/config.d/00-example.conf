# Example host file — seeded into ~/.ssh/config.d/ by install.sh on a fresh
# machine. Copy/rename to e.g. 10-myhosts.conf and edit. Files load in lexical
# order (numeric prefix). Your real files are gitignored; only this one is tracked.
#
# Host my-server
#     HostName 10.0.0.12
#     User deploy
#     ProxyJump my-bastion
#     IdentityFile ~/.ssh/id_ed25519
